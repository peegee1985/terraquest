diff --git a/node_modules/@convex-dev/auth/dist/server/implementation/users.js b/node_modules/@convex-dev/auth/dist/server/implementation/users.js
index 24564a2..b69c20c 100644
--- a/node_modules/@convex-dev/auth/dist/server/implementation/users.js
+++ b/node_modules/@convex-dev/auth/dist/server/implementation/users.js
@@ -15,6 +15,7 @@ async function defaultCreateOrUpdateUser(ctx, existingSessionId, existingAccount
         logWithLevel(LOG_LEVELS.DEBUG, "Using custom createOrUpdateUser callback");
         return await config.callbacks.createOrUpdateUser(ctx, {
             existingUserId,
+            existingSessionId,
             ...args,
         });
     }
diff --git a/node_modules/@convex-dev/auth/dist/server/types.d.ts b/node_modules/@convex-dev/auth/dist/server/types.d.ts
index b0b61e1..f5acd1b 100644
--- a/node_modules/@convex-dev/auth/dist/server/types.d.ts
+++ b/node_modules/@convex-dev/auth/dist/server/types.d.ts
@@ -149,6 +149,15 @@ export type ConvexAuthConfig = {
              * this is the existing user ID linked to that account.
              */
             existingUserId: GenericId<"users"> | null;
+            /**
+             * TerraQuest patch: the `authSessions` document ID of the session
+             * that was active when this sign-in flow began (e.g. the anonymous
+             * guest session that initiated an OAuth linking flow), if any.
+             * Look it up with `ctx.db.get(existingSessionId)` to get `userId` —
+             * `getAuthUserId(ctx)` cannot resolve this during an OAuth callback,
+             * since that request has no bearer token attached.
+             */
+            existingSessionId?: GenericId<"authSessions"> | null;
             /**
              * The provider type or "verification" if this callback is called
              * after an email or phone token verification.
